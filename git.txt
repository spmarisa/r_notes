git init

git add .

git add .gitignore

git commit -m "initial commit"

git remote add origin git@github.com:username/new_repo
git remote add origin https://github.com/spmarisa/depot.git

git push origin master

git checkout -b develop